spring:
  profiles:
    active: local
  batch:
    job:
      names: ${job.name:NONE}Job

server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      #enabled: true
      force: true